export interface EmailOptions {
  EMAI_KEY: string;
  MAIL_DOMAIN: string;
  MAIL_FROM: string;
}

export interface Emailvalues {
  subject: string;
  template: string;
  to: string;
}

export interface TemplateValues {
  test: string;
  user: string;
  href: string;
}
